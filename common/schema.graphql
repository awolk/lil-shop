type Item {
  id: ID!
  name: String!
  costCents: Int!
}

type LineItem {
  id: ID!
  item: Item!
  quantity: Int!
}

type Cart {
  id: ID!
  lineItems: [LineItem!]!
}

type Query {
  items: [Item!]!
  cart(id: ID!): Cart!
}

type Mutation {
  # returns new cart ID
  newCart: ID!
  # returns line item ID
  addItemToCart(itemID: ID!, quantity: Int!, cartID: ID!): ID!
}
